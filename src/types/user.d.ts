declare namespace User {
  export interface Details {
    _id: string
    full_name: string
    address: string
    phone: string
    email: string
    role: string
    createdAt: string
    updatedAt: string
  }
}
